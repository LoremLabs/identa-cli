# Configuration for npm/pnpm package resolution

# For local development, you can use pnpm overrides in package.json:
# "pnpm": {
#   "overrides": {
#     "@ident-agency/core": "file:../ident-agency-sdk/packages/core"
#   }
# }

# Ensure we get the latest versions from npm
prefer-workspace-packages=false